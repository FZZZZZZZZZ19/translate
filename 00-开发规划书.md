# 屏幕截图 OCR 翻译工具 —— 开发规划书（v1.1）

> 版本：v1.1（技术方案变更：移除 EasyOCR，改千问视觉模型一条龙）  |  立项：2026-08-04  |  编制：Hermes（项目经理）  |  开发执行：Claude Code（全权）

---

## 0. 版本变更记录

| 版本 | 日期 | 变更内容 | 原因 |
|---|---|---|---|
| v1.0 | 2026-08-04 | 初版：EasyOCR 本地识别 + 千问翻译两步 | — |
| v1.1 | 2026-08-04 | **移除 EasyOCR/PyTorch**，改为千问视觉模型（qwen-vl 系列）对截图**一次调用**完成「英文识别 + 中文翻译 + 文本坐标」；删除 `ocr_engine.py`/`translator.py`，合并为 `qwen_client.py` | 压缩打包体积（500MB+ → ≤60MB）；无需本地 OCR 依赖；识别翻译一步到位 |

---

## 1. 项目概述

### 1.1 项目名称
ScreenCaptureOCR Translator（屏幕截图 AI 翻译工具）

### 1.2 项目定位
Windows 桌面常驻小工具：用户按下自定义全局快捷键，在屏幕上拖拽拉出矩形选区，工具将选区截图上传用户自备的通义千问视觉模型（qwen-vl 系列）API，**一次请求**同时完成：识别英文 → 翻译中文 → 输出每行文本的屏幕坐标；随后以覆盖层形式把中文译文绘制回原文位置；按 ESC 退出本次覆盖模式；再次按快捷键重复流程。

### 1.3 目标用户与使用场景
- 常读英文软件界面、文档、游戏画面，希望「就地翻译、不切换窗口」的用户。
- 典型场景：翻译软件对话框、英文网页局部、游戏内英文 UI、PDF 中无法复制的文字段落。

### 1.4 项目目标（SMART）
1. 交付可双击运行的 Windows 可执行文件（.exe），目标机器无需安装 Python。
2. **打包体积 ≤ 60MB**（关键约束：不用 EasyOCR/PyTorch 等重型本地依赖）。
3. 核心链路「快捷键 → 选区 → AI 识别翻译 → 覆盖 → ESC 退出 → 再触发」完整可跑通。
4. 用户自行提供千问 API Key，工具不内置付费密钥。
5. 翻译结果覆盖在原文位置，视觉上「原文被替换成中文」。

---

## 2. 角色与分工

| 角色 | 承担者 | 职责 |
|---|---|---|
| 项目经理 | Hermes | 需求分析、技术选型、架构设计、里程碑定义、验收标准制定 |
| 项目记录者 | Hermes | 规划书、开发计划、开发记录，追踪进度与问题 |
| 开发执行 | Claude Code | 全权负责编码、依赖安装、联调、PyInstaller 打包与验证 |
| 产品验收 | 用户 | 按验收清单实测，反馈问题 |

**协作模式**：Hermes 只做规划与记录，不写业务代码；Claude Code 在 `C:\Users\lzega\Desktop\translate` 目录自主开发，按 `CLAUDE.md`（v1.1）规格执行，每批完成后在 `02-项目开发记录.md` 留档。

---

## 3. 需求分析

### 3.1 功能需求（FR）

| 编号 | 需求 | 详细说明 | 优先级 |
|---|---|---|---|
| FR-1 | 全局自定义快捷键 | 常驻托盘，设置中可自定义触发快捷键（默认 `Ctrl+Alt+T`），全局生效（前台是任何其他软件时也能触发） | P0 |
| FR-2 | 选区截图（左上角开拉四边形） | 触发后全屏半透明遮罩，左键按下为矩形左上角，拖到右下角松开成选区；选区外变暗、选区内清晰并显示尺寸 | P0 |
| FR-3 | 选区取消 | 拖拽中按 ESC 或右键取消，回到待机 | P0 |
| FR-4 | AI 识别 + 翻译一条龙 | 选区图上传千问视觉模型，**一次调用**返回每行：英文原文、中文译文、包围盒四角点坐标（屏幕绝对坐标） | P0 |
| FR-5 | 译文覆盖原位置 | 按坐标绘制覆盖层：底色覆盖原文、其上绘制中文译文，字号自适应选区宽度，支持多行 | P0 |
| FR-6 | ESC 退出模式 | 覆盖层显示期间按 ESC 一键退出，覆盖层消失，屏幕恢复原样 | P0 |
| FR-7 | 重复操作 | 退出后再次按快捷键重新进入流程 | P0 |
| FR-8 | 设置界面 | API Key、视觉模型选择（可编辑下拉，默认 qwen-vl-plus）、快捷键自定义、覆盖层样式（背景/文字色/边距/最小字号） | P1 |
| FR-9 | 配置持久化 | 设置存本地 JSON，重启不丢 | P1 |
| FR-10 | 状态反馈 | 识别翻译中/完成/失败给出可见反馈（托盘气泡或小浮动提示），失败给出明确原因（网络/Key 无效/图片过大/模型返回异常） | P1 |
| FR-11 | 打包 exe | PyInstaller 打包为绿色免安装可执行文件，体积 ≤60MB | P0 |

### 3.2 非功能需求（NFR）

| 编号 | 类别 | 要求 |
|---|---|---|
| NFR-1 | 性能 | 选区后单次 API 往返 ≤ 8 秒（qwen-vl-plus 通常 3-5s）；覆盖层即时显示；连续操作不卡 UI |
| NFR-2 | 体积 | 打包产物 ≤ 60MB；运行时依赖仅 PyQt5 / keyboard / requests |
| NFR-3 | 兼容性 | Windows 10/11 x64；高分屏（125%/150%）选区与覆盖位置不错位；支持多显示器（MVP 主屏） |
| NFR-4 | 可靠性 | 断网、Key 失效、限流、模型返回异常均不崩溃，给出可读提示后回到待机 |
| NFR-5 | 安全 | API Key 仅存本机配置文件，不硬编码、不写日志 |
| NFR-6 | 可维护性 | 模块化，核心逻辑与 UI 解耦；视觉/翻译均走千问一家（「一条路服务」），接口预留切换 OpenAI 兼容供应商的可能 |
| NFR-7 | 费用可控 | 每次操作消耗图片+文本 token，README 说明计费方式；模型可选更便宜款 |

### 3.3 边界与不做（Out of Scope）
- 不做本地 OCR 引擎（EasyOCR 等一律不引入，体积优先）。
- 不做截屏图片本地保存/历史管理。
- 不做开机自启（托盘菜单预留入口即可）。
- 不支持 Mac/Linux。

---

## 4. 技术选型

| 领域 | 选型 | 理由 |
|---|---|---|
| GUI 框架 | PyQt5 | 用户指定；QPainter 适合覆盖层绘制 |
| AI 识别+翻译 | 千问 DashScope 视觉模型（OpenAI 兼容模式） | 用户自备 Key；一条路服务（识别+翻译同一家）；**替代 EasyOCR 以压缩体积** |
| 默认模型 | `qwen-vl-plus`（可编辑，支持 qwen-vl-max / qwen2.5-vl / qwen3-vl 等） | 快、便宜、支持图片输入与坐标输出；具体以阿里云百炼控制台为准 |
| HTTP 客户端 | requests | 轻量，OpenAI 兼容接口直接 POST |
| 全局热键 / ESC | `keyboard` 库 | Windows 下成熟；全局组合键 + 全局 ESC 监听 |
| 截屏 | PyQt5 `QScreen.grabWindow(0)` | 原生多屏与 DPI 支持 |
| 图片编码/缩放 | Qt 内置（QImage/QBuffer） | **不引入 Pillow/numpy**，进一步压缩依赖 |
| 打包 | PyInstaller（onedir 模式） | PyQt5 支持成熟；体积预期 40-60MB |
| Python 版本 | 3.11.x（64 位） | PyQt5 支持最稳 |
| 依赖管理 | venv + `requirements.txt` | 依赖极少（3 个运行时包），干净可控 |

**体积对比**：v1.0（EasyOCR+PyTorch）≈ 500MB-1GB；v1.1（纯千问）≈ 40-60MB，且无模型下载、无 GPU 顾虑、无杀软误报率高的 torch 动态库。

---

## 5. 系统架构

### 5.1 总体架构

```
┌────────────────────────────────────────────────────────────┐
│                       main.py（入口）                        │
│   QApplication + 系统托盘 + 全局热键注册 + 状态机调度           │
└──────────┬─────────────────────────────────────────────────┘
           │
   ┌───────┴───────────────────────────────────────────┐
   │                  Pipeline（流程编排）                │
   │   SELECTING → RECOGNIZING(识别+翻译) → OVERLAYING   │
   └───┬──────────┬───────────────────────┬─────────────┘
       │          │                       │
   ┌───▼───┐ ┌────▼──────────┐     ┌──────▼───────┐
   │ 选区遮罩 │ │  qwen_client  │     │  覆盖层窗口    │
   │ selection│ │ （一条龙：识别+  │     │ result_overlay│
   │ _overlay │ │  翻译+坐标JSON）│     └──────┬───────┘
   └───┬───┘ └────┬──────────┘            │
       │          │                       │
       └── 共享：screen.py（截屏 + 物理/逻辑坐标换算 + 图片缩放编码）──┘
```

### 5.2 目录结构（目标）

```
translate/
├── main.py                    # 程序入口：托盘、热键、状态机
├── app/
│   ├── __init__.py
│   ├── config.py              # 配置读写（JSON）
│   ├── hotkey.py              # 全局热键 + ESC 监听（keyboard）
│   ├── screen.py              # 截屏、DPI/坐标换算、图片裁剪/缩放/base64 编码
│   ├── selection_overlay.py   # 选区遮罩窗口
│   ├── result_overlay.py      # 译文覆盖层窗口
│   ├── qwen_client.py         # 千问客户端：视觉识别+翻译一条龙（核心模块）
│   ├── pipeline.py            # 流程编排 + QThread 工作线程
│   └── settings_dialog.py     # 设置对话框
├── assets/icon.ico
├── requirements.txt           # 仅 PyQt5 / keyboard / requests
├── ocr_translator.spec
├── build.bat
├── README.md
└── 文档：00-开发规划书.md / 01-开发计划.md / CLAUDE.md / 02-项目开发记录.md
```

### 5.3 核心流程（时序）

```
用户按全局快捷键
  → 热键回调（keyboard 线程）→ Qt 信号切主线程
  → 隐藏任何残留覆盖层
  → 全屏截屏（grabWindow）保存为底图
  → 显示选区遮罩（全屏半透明，底图垫底）
  → 用户拖拽矩形（ESC/右键取消）
  → 从底图裁剪选区（物理像素 QImage）
  → 工作线程 qwen_client.recognize_and_translate(image, 选区偏移):
      1) 缩放至最长边 ≤2048（记录缩放比）
      2) QBuffer 编码 PNG → base64 data URI
      3) POST 千问 /chat/completions（qwen-vl，图片+结构化 prompt）
      4) 解析 JSON 数组（每行：text / bbox / translation）
      5) bbox × 缩放比 + 选区偏移 → 屏幕绝对物理坐标
  → 主线程显示译文覆盖层（按 bbox 画背景块 + 中文译文）
  → 用户按 ESC（全局钩子）→ 隐藏覆盖层，回待机
  → 再按快捷键 → 重复
```

---

## 6. 关键技术方案（决策记录）

### 6.1 全局热键与 ESC：`keyboard` 库（同 v1.0，不变）
- `keyboard.add_hotkey(快捷键)` 注册全局组合键；`keyboard.add_hotkey('esc', ...)` 监听 ESC（覆盖层鼠标穿透时唯一收键途径）。
- 回调运行在钩子线程，**必须** `QTimer.singleShot(0, fn)` 切回 Qt 主线程。
- 坑：管理员权限下对非管理员进程可能失灵 → 提示用户普通权限运行；热键冲突注册失败需提示。

### 6.2 截屏与选区（同 v1.0，不变）
- `QScreen.grabWindow(0)` 抓全屏；选区遮罩全屏无边框置顶半透明，`QRect(start, current).normalized()` 实现「左上角开拉四边形」；`物理 = 逻辑 × devicePixelRatio` 换算全部收敛 `screen.py`。

### 6.3 千问视觉识别 + 翻译一条龙（v1.1 核心）
- **端点**：`POST https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`，`Authorization: Bearer <Key>`。
- **图片输入**：OpenAI 兼容 content 数组格式——`[{"type":"text","text":...},{"type":"image_url","image_url":{"url":"data:image/png;base64,..."}}]`；用 `response_format: {"type":"json_object"}` 强制 JSON 输出。
- **图片预处理**：最长边 > 2048px 时等比缩放（Qt.SmoothTransformation），**记录缩放比**，模型返回坐标必须按缩放比换算回原图坐标——这是坐标准确性的关键。
- **结构化 prompt**：告知图片像素尺寸，要求逐行输出 JSON 数组，每项 `{"text": 英文原文, "bbox": [[x1,y1],[x2,y2],[x3,y3],[x4,y4]], "translation": 简体中文}`；bbox 为四角点像素坐标（左上→右上→右下→左下），须完整包裹整行文字；只输出 JSON。
- **解析鲁棒性**：剥离 markdown 代码块（```json ... ```）后定位首个 `[` 与末个 `]` 提取；json.loads 失败重试 1 次；字段缺失/bbox 越界/面积为 0 的行丢弃；translation 缺失或为空用 text 兜底。
- **坐标换算链**：模型 bbox（上传图像素）→ ×缩放比 → 原图物理像素 → +选区左上角物理坐标 → 屏幕绝对坐标 → 覆盖层经 screen.py 转逻辑坐标绘制。
- **错误分类**：401/403→「API Key 无效或无权限」；429→「请求过于频繁」；超时→「服务超时」；JSON 解析失败→「模型返回格式异常」；全部不崩溃、原文兜底、回待机。
- **限制**：单次请求行数不做硬限制（模型自适应），但图片 token 与体积相关，README 说明计费；温度 0.1；超时 (10, 60)s，仅 5xx/超时重试 1 次。

### 6.4 覆盖层渲染（同 v1.0，不变）
- 全屏透明窗口：`WindowTransparentForInput`（鼠标穿透）+ 置顶 + 不抢焦点；每行 bbox 画背景块（默认白底 + 4px padding）遮盖原文，其上绘制中文译文。
- 字号自适应：QFontMetrics 测量，先折行（中文按字符、英文按词）再降字号，下限 9pt；垂直居中；抗锯齿。
- 全部 OCR+翻译完成后再一次性 show（防闪烁）；每次流程开始先 hide 旧层。

### 6.5 高分屏 / 多显示器（同 v1.0，不变）
- `AA_EnableHighDpiScaling` + `AA_UseHighDpiPixmaps`；所有换算收敛 `screen.py`；MVP 主屏，预留多屏。

### 6.6 线程模型（同 v1.0，不变）
- 主线程：Qt 事件循环/UI；keyboard 钩子线程经信号转主线程；工作线程（QThread）跑网络请求；信号回传结果；状态机 `IDLE → SELECTING → RECOGNIZING → OVERLAYING → IDLE`，任一失败回 IDLE 并托盘提示；禁止跨线程操作 QWidget。

---

## 7. 风险与对策（v1.1 更新）

| 风险 | 影响 | 对策 |
|---|---|---|
| 大模型 bbox 坐标有像素级偏差 | 译文位置轻微偏移 | 覆盖层 padding 4px + 背景块完整遮盖原文；prompt 强调「完整包裹整行文字」；偏差属可接受范围（≤5px） |
| 模型漏行 / 多行 / 行序错乱 | 译文缺行或错位 | prompt 严格要求逐行输出；缺失行按行序兜底为空（该区域不覆盖）；覆盖层按行独立绘制，单行错误不影响整体 |
| 单次图片过大被 API 拒绝 | 请求失败 | 最长边缩放到 2048；PNG 编码；失败提示 |
| 必须联网（无本地识别） | 断网不可用 | 明确提示「网络错误」；这是体积优先方案的取舍，README 说明 |
| API 费用（图片 token） | 使用成本 | 模型可选手动输入更便宜款；README 计费说明 |
| key 失效/限流/超时 | 翻译失败 | 错误分类 + 原文兜底 + 不崩溃 |
| PyInstaller 打包 PyQt5 缺插件 | 启动崩溃 | spec 常规 collect；打包后干净环境实测 |
| 杀软误报 | 用户不敢用 | onedir + 无壳 + README 白名单指引 |
| 键盘钩子在管理员/杀软环境失灵 | 热键无效 | 普通权限运行提示；支持改键 |

---

## 8. 里程碑（v1.1 调整）

| 里程碑 | 内容 | 出口标准 |
|---|---|---|
| M0 立项 | 规划书/计划/CLAUDE.md/记录就绪 | 文档齐备，目录初始化（git init、venv） |
| M1 骨架可运行 | 托盘、热键、选区遮罩、选区图裁剪保存 | 热键→遮罩→拖拽→生成选区 PNG |
| M2 识别翻译一条龙 | qwen_client：缩放→编码→请求→JSON 解析→坐标换算 | 真实 Key 对截图返回逐行 text/bbox/translation 且坐标正确 |
| M3 全流程覆盖 | 覆盖层回填 + ESC 退出 + 可循环 | 完整流程一遍走通无崩溃 |
| M4 打磨 | 设置界面、状态提示、异常处理、DPI/多屏修正、单测 | 验收清单 80% 通过 |
| M5 打包发布 | PyInstaller onedir、干净环境实测、README、体积 ≤60MB | exe 干净 Win10/11 全流程可用 |

---

## 9. 总体验收标准（v1.1）

1. 双击 exe 启动，托盘图标出现，无控制台黑窗。
2. 默认快捷键触发全屏选区遮罩，拖拽出矩形，选区外变暗。
3. 松开鼠标后 3-10 秒内（网络往返），原文位置出现中文译文覆盖，原文被遮盖。
4. 覆盖层鼠标可穿透，按 ESC 立即退出；再次快捷键可重复（连续 10 次无崩溃）。
5. 设置中可改 API Key / 模型 / 快捷键 / 样式，保存重启后生效。
6. 断网、Key 错误、模型返回异常均友好提示，不崩溃。
7. 125%/150% 缩放下选区与译文位置不错位。
8. **打包产物总体积 ≤ 60MB**，干净 Windows 机器双击可用。

---

## 10. 交付物清单

1. 源代码（按 5.2 目录结构，含注释）
2. `requirements.txt`（PyQt5 / keyboard / requests）
3. `ocr_translator.spec` + `build.bat`
4. `dist/` 可运行目录（≤60MB）
5. `README.md`（安装、首次使用、API Key 获取、模型说明、计费、常见问题）
6. 开发记录 `02-项目开发记录.md`（每批次留档）

---

*本文档由 Hermes 作为项目经理编制；函数级规格见 `CLAUDE.md`（v1.1），任务拆分见 `01-开发计划.md`（v1.1）。*
