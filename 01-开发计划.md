# 屏幕截图 AI 翻译工具 —— 开发计划（v1.1）

> 版本：v1.1（同步 v1.1 方案：移除 EasyOCR，千问视觉一条龙）  |  编制：Hermes（项目经理）  |  执行：Claude Code  |  关联：`00-开发规划书.md`、`CLAUDE.md`、`02-项目开发记录.md`

---

## 1. 开发原则（对 Claude Code 的硬性约束）

1. **目录**：全部代码在 `C:\Users\lzega\Desktop\translate` 下，结构遵循 `CLAUDE.md` 第 3 节。
2. **流程**：按批次 A→F 顺序执行，每批：跑通该批验收 → `02-项目开发记录.md` 追加记录 → 下一批。
3. **不跳步**：每批必须实际运行验证（`python main.py` / pytest），不能只写完代码。
4. **依赖纪律（v1.1 关键）**：运行时依赖**仅限** PyQt5 / keyboard / requests。**禁止**引入 easyocr / torch / torchvision / paddleocr / Pillow / numpy / opencv 等任何重型或多余库（图片缩放编码用 Qt 内置完成）。
5. **代码规范**：类型标注 + 中文 docstring；坐标换算全部走 `app/screen.py`；禁止跨线程操作 QWidget；禁止 print 调试残留（用 logging）。
6. **git**：`git init`，每批一个 commit（`feat: 批次X …`）；`config.json`、`venv`、`dist`、`build` 入 .gitignore。
7. **API Key**：不硬编码、不写日志；测试用用户提供的真实 Key，验证后不留在代码里。
8. **打包**：仅全部批次完成后执行批次 F，打包前跑通全部手工验收。
9. **模型名**：默认 `qwen-vl-plus`；设置支持可编辑下拉；具体可用模型以阿里云百炼控制台为准，联调时如遇模型名失效及时在记录中更新。

---

## 2. 批次任务分解（v1.1）

### 批次 A：项目骨架 + 托盘 + 全局热键 + 选区截图

**目标**：常驻托盘、快捷键弹全屏遮罩、拖拽矩形、松开生成选区图（临时目录验证）。

**任务清单**：
- [ ] A1 创建 venv（Python 3.11）、安装 PyQt5 / keyboard / requests；`git init` + 目录结构 + requirements.txt + .gitignore
- [ ] A2 `main.py`：HighDPI 属性（创建 QApplication 前）、QSystemTrayIcon（菜单：截图翻译/设置/退出；双击托盘=触发）、退出清理（unregister 钩子 + join 线程）
- [ ] A3 `app/hotkey.py`：keyboard 全局热键（默认 `ctrl+alt+t`）+ ESC 监听；回调 QTimer.singleShot 切主线程；重新注册先 unregister
- [ ] A4 `app/screen.py`：grab_fullscreen()、ScreenMapper（逻辑↔物理换算）、crop 返回 QImage
- [ ] A5 `app/selection_overlay.py`：全屏半透明遮罩（底图垫底）、拖拽选区 normalized、实时尺寸、ESC/右键取消
- [ ] A6 联调：热键→遮罩→选区→选区 QImage 存临时 PNG→日志输出选区矩形

**验收**：
- 托盘图标可见；`Ctrl+Alt+T` 弹遮罩；拖拽选区外变暗；松开后临时目录出现选区 PNG；ESC 取消无残留。
- 连续触发/取消 10 次无崩溃、无残留窗口。

**常见坑**：keyboard 回调线程→Qt 主线程转发；grabWindow 返回物理像素尺寸。

---

### 批次 B：千问视觉一条龙（核心）

**目标**：选区图 → 一次 API 调用 → 每行 {text, bbox(屏幕绝对坐标), translation}。

**任务清单**：
- [ ] B1 `app/qwen_client.py`：VisionLine dataclass；QwenError（code 区分 auth/rate_limit/timeout/network/parse/bad_response）
- [ ] B2 图片预处理：QImage 最长边 >2048 等比缩放（SmoothTransformation）+ 记录缩放比；QBuffer 编码 PNG → base64 data URI
- [ ] B3 请求构造：content 数组（text prompt + image_url data URI）、response_format json_object、temperature 0.1、timeout (10,60)、5xx/超时重试 1 次
- [ ] B4 结构化 prompt（按 CLAUDE.md 4.6 的模板）：图片尺寸、逐行 JSON 数组（text/bbox/translation）、bbox 四角点顺序与范围约束
- [ ] B5 解析：剥 ```json 代码块 → 定位首个 [ 末个 ] → json.loads；失败重试 1 次；字段缺失/bbox 越界/空 translation 处理（兜底/丢弃）
- [ ] B6 坐标换算链：模型 bbox × 缩放比 + 选区偏移 → 屏幕绝对物理坐标（在 qwen_client 内完成）
- [ ] B7 联调：真实 Key 对多行英文截图验证逐行 text/bbox/translation；坐标与原文对位目测核对

**验收**：
- 对一张含 5+ 行英文的截图，返回每行原文+译文+坐标；译文为简体中文；坐标与原文位置基本吻合（≤5px 偏差可接受）。
- 坏 Key（401）、断网、超时分别得到可读错误；空选区/无文本行返回 [] 不崩溃。
- 图片缩放后坐标换算正确（用 200% 大图验证，译文仍覆盖原文）。

**常见坑**：模型返回 markdown 包裹的 JSON；bbox 是上传图像素（必须先乘缩放比再加偏移）；response_format json_object 要求 prompt 含「json」字样。

---

### 批次 C：配置管理 + 设置界面

**目标**：设置可改、持久化、生效。

**任务清单**：
- [ ] C1 `app/config.py`：AppConfig（api_key/model/hotkey/overlay 样式/最大边长）、ConfigManager（%APPDATA%/ScreenCaptureOCR/config.json，损坏容错、原子写）；config.json 入 .gitignore
- [ ] C2 `app/settings_dialog.py`：API Key（密码框+显示）、模型**可编辑下拉**（qwen-vl-plus/qwen-vl-max/qwen2.5-vl-72b-instruct/qwen3-vl-plus）、快捷键捕获（keyboard hook 捕获组合键）、背景/文字色、padding、最小字号、最大边长
- [ ] C3 保存生效：cfg.save + 热键重新注册（失败回滚并提示）
- [ ] C4 托盘「设置」菜单入口接通

**验收**：
- 各项设置保存后重启读取一致；改快捷键立即生效；改模型/Key 后下一次操作使用新值。

---

### 批次 D：译文覆盖层 + ESC 退出 + 全流程

**目标**：译文绘制回屏幕原文位置，ESC 退出，可循环。

**任务清单**：
- [ ] D1 `app/result_overlay.py`：全屏透明窗口（WA_TranslucentBackground + WindowTransparentForInput + 置顶 + 不抢焦点）
- [ ] D2 绘制：每行 bbox → 背景块（padding 4px 默认）+ 中文译文；字号自适应（折行→降字号→下限 9pt）；垂直居中；抗锯齿
- [ ] D3 ESC 全局监听 → 隐藏覆盖层回 IDLE；再次热键可重入
- [ ] D4 状态机完整：IDLE→SELECTING→RECOGNIZING→OVERLAYING→IDLE，失败回退 + 托盘提示；防重入（处理中忽略热键）
- [ ] D5 防闪烁：全部就绪后一次性 show；每次触发先 hide 旧层

**验收**：
- 真实软件界面截图翻译：译文覆盖在原文上、位置准确、可读；多行布局不乱。
- 覆盖层鼠标穿透；ESC 立即消失；连续 5 轮「热键→选区→翻译→ESC」无崩溃无窗口泄漏。
- 模型返回部分行时，缺失区域不覆盖、整体不错位。

---

### 批次 E：打磨

**目标**：体验完整、异常可感知。

**任务清单**：
- [ ] E1 状态反馈：托盘气泡「AI 识别翻译中…/完成/失败原因」；失败含错误分类文案
- [ ] E2 异常全面化：网络、坏 Key、限流、超时、JSON 解析失败、空结果、选区过小（<10px）等分支
- [ ] E3 多显示器与 DPI：100%/125%/150% 缩放主屏全流程验证；预留多屏枚举
- [ ] E4 单元测试（pytest）：坐标换算、缩放比换算、JSON 解析（含 markdown 包裹/缺字段/越界）、字号自适应；错误分类
- [ ] E5 性能：qwen_client 复用连接（requests.Session）；内存占用确认

**验收**：
- 全场景错误提示友好；pytest 通过；125%/150% 缩放不错位。

---

### 批次 F：PyInstaller 打包 + 发布

**目标**：≤60MB 绿色 exe，干净机器可跑。

**任务清单**：
- [ ] F1 `ocr_translator.spec`：onedir、windowed、PyQt5 插件收集、icon；排除测试/无用数据
- [ ] F2 `build.bat`：激活 venv → `pyinstaller --clean ocr_translator.spec`
- [ ] F3 开发机自检：dist 内 exe 全流程过一遍
- [ ] F4 干净环境验证：无 Python 目标机：启动、热键、选区、AI 翻译、覆盖、ESC 全流程
- [ ] F5 体积确认：统计 dist 总大小 ≤ 60MB；超标则精简（排除 Qt 无用模块/插件）并记录
- [ ] F6 `README.md`：安装/使用/首次配置/API Key 获取（阿里云百炼控制台）/模型与计费说明/断网与体积取舍说明/常见问题（杀软、权限）
- [ ] F7 收尾：开发记录总结、验收清单签署

**验收**：
- 干净 Win10/11 x64 双击 exe 全流程可用；启动无控制台；体积 ≤ 60MB；连续使用 30 分钟内存稳定。

---

## 3. 依赖清单（requirements.txt —— v1.1）

```
PyQt5>=5.15.10
keyboard>=0.13.5
requests>=2.31
pytest>=8.0   # 仅开发用，可放 requirements-dev.txt
```

> **纪律**：除上述外不得新增运行时依赖。图片缩放/编码用 Qt（QImage/QBuffer）；HTTP 用 requests；不用 Pillow、不用 numpy、不用 opencv、不用任何 OCR 框架。

---

## 4. 里程碑对照表（v1.1）

| 里程碑 | 对应批次 | 备注 |
|---|---|---|
| M0 立项 | 文档批次 | 规划/计划/CLAUDE.md/记录已升级 v1.1 |
| M1 骨架可运行 | 批次 A | — |
| M2 识别翻译一条龙 | 批次 B | 核心：qwen_client |
| M3 全流程覆盖 | 批次 C+D | 配置/设置 + 覆盖层 + ESC |
| M4 打磨 | 批次 E | 异常/提示/DPI/测试 |
| M5 打包发布 | 批次 F | 体积 ≤60MB |

---

## 5. 手工验收清单（最终发布前逐项打勾）

- [ ] 双击 exe 启动，托盘图标可见，无控制台
- [ ] 默认快捷键全屏遮罩、拖拽选区、选区外变暗
- [ ] 松开后 3-10 秒译文覆盖在原文位置，底色遮盖原文
- [ ] 覆盖层鼠标穿透；ESC 立即退出；连续 10 次无崩溃
- [ ] 设置：改 Key/模型/快捷键/样式后重启生效
- [ ] 断网、坏 Key、模型返回异常 → 友好提示，不崩溃
- [ ] 125%/150% 缩放下选区与译文位置准确
- [ ] 打包体积 ≤ 60MB；托盘退出后进程完全结束

---

*本计划由 Hermes（项目经理）编制，Claude Code 按批次执行；执行情况由 Hermes（项目记录者）在 `02-项目开发记录.md` 留档。*
