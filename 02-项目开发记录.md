# 项目开发记录 —— ScreenCaptureOCR Translator

> 记录者：Hermes（项目记录者）  |  开发执行：Claude Code  |  本文件是开发过程的唯一留档，每批次开发完成后由 Claude Code 追加一节。

---

## 记录格式模板（新增批次时复制本节）

```markdown
## [批次X] <名称> —— <日期>

- **执行者**：Claude Code（session: <可选>）
- **涉及文件**：<列出>  
- **完成项**：
  - [ ] <任务1>
  - [ ] <任务2>
- **验证结果**：
  - <实测输出/截图路径/日志要点>  
- **问题与决策**：
  - <遇到的坑及处理方式>  
- **遗留事项**：
  - <未完成/下批注意>
- **验收结论**：✅ 通过 / ⚠️ 部分通过（说明） / ❌ 未通过（说明）
```

---

## 立项记录 —— 2026-08-04

- **事件**：项目立项（v1.0 方案）。
- **产出**：规划书/计划/CLAUDE.md/记录模板。
- **决策**：PyQt5 + EasyOCR + 千问 DashScope + keyboard + PyInstaller(onedir)。

---

## 技术方案变更记录 —— 2026-08-04（v1.0 → v1.1）

- **决策**：**移除 EasyOCR / PyTorch**，全部 AI 能力（英文识别 + 中文翻译 + 文本坐标）改由千问视觉模型（qwen-vl 系列）**一次 API 调用**完成，「一条路服务」。
- **原因**：压缩打包体积（500MB+ → ≤60MB）、去掉 torch 打包痛点、识别翻译一步到位减少等待。
- **架构影响**：
  - 删除 `app/ocr_engine.py`、`app/translator.py` → 合并为 `app/qwen_client.py`（`recognize_and_translate(image, offset) -> list[VisionLine]`）。
  - `app/screen.py` 裁剪返回 QImage（不再需要 numpy 数组）；新增图片缩放 + base64 编码职责。
  - 状态机：`IDLE → SELECTING → RECOGNIZING → OVERLAYING → IDLE`（原 OCRING/TRANSLATING 合并为 RECOGNIZING）。
  - 依赖缩减为：PyQt5 / keyboard / requests（无 Pillow、无 numpy）。
  - 默认模型 `qwen-vl-plus`，设置中可编辑下拉（qwen-vl-max / qwen2.5-vl / qwen3-vl 系列）。
  - 里程碑改为 M0-M5（M2 = 识别翻译一条龙）。
- **已同步更新**：`00-开发规划书.md` / `01-开发计划.md` / `CLAUDE.md` 全部升级至 v1.1。

---

## 待填批次（Claude Code 按顺序追加）

- [ ] 批次 A：项目骨架 + 托盘 + 全局热键 + 选区截图
- [ ] 批次 B：千问视觉一条龙（缩放/编码/请求/JSON 解析/坐标换算）
- [ ] 批次 C：配置管理 + 设置界面
- [ ] 批次 D：译文覆盖层 + ESC 退出 + 全流程
- [ ] 批次 E：打磨（异常/状态提示/DPI/单元测试）
- [ ] 批次 F：PyInstaller 打包 + 发布（体积 ≤60MB）

---

## 验收记录（最终发布前签署）

- [ ] 双击 exe 启动，托盘图标可见，无控制台
- [ ] 默认快捷键全屏遮罩、拖拽选区、选区外变暗
- [ ] 松开后 3-10 秒译文覆盖在原文位置，底色遮盖原文
- [ ] 覆盖层鼠标穿透；ESC 立即退出；连续 10 次无崩溃
- [ ] 设置修改后重启生效
- [ ] 断网 / 坏 Key / 模型返回异常 → 友好提示不崩溃
- [ ] 125%/150% 缩放下选区与译文不错位
- [ ] 打包体积 ≤ 60MB；托盘退出后进程完全结束

*签署人：用户  |  日期：____-__-__*
