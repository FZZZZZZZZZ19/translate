# 项目开发记录 —— ScreenCaptureOCR Translator

> 记录者：Hermes（项目记录者）  |  开发执行：Claude Code  |  本文件是开发过程的唯一留档，每批次开发完成后由 Claude Code 追加一节。

---

## 记录格式模板（新增批次时复制本节）

```markdown
## [批次X] <名称> —— <日期>

- **执行者**：Claude Code（session: <可选>）
- **涉及文件**：<列出>  
- **完成项**：
  - [ ] <任务1>
  - [ ] <任务2>
- **验证结果**：
  - <实测输出/截图路径/日志要点>  
- **问题与决策**：
  - <遇到的坑及处理方式>  
- **遗留事项**：
  - <未完成/下批注意>
- **验收结论**：✅ 通过 / ⚠️ 部分通过（说明） / ❌ 未通过（说明）
```

---

## 立项记录 —— 2026-08-04

- **事件**：项目立项（v1.0 方案）。
- **产出**：规划书/计划/CLAUDE.md/记录模板。
- **决策**：PyQt5 + EasyOCR + 千问 DashScope + keyboard + PyInstaller(onedir)。

---

## 技术方案变更记录 —— 2026-08-04（v1.0 → v1.1）

- **决策**：**移除 EasyOCR / PyTorch**，全部 AI 能力（英文识别 + 中文翻译 + 文本坐标）改由千问视觉模型（qwen-vl 系列）**一次 API 调用**完成，「一条路服务」。
- **原因**：压缩打包体积（500MB+ → ≤60MB）、去掉 torch 打包痛点、识别翻译一步到位减少等待。
- **架构影响**：
  - 删除 `app/ocr_engine.py`、`app/translator.py` → 合并为 `app/qwen_client.py`（`recognize_and_translate(image, offset) -> list[VisionLine]`）。
  - `app/screen.py` 裁剪返回 QImage（不再需要 numpy 数组）；新增图片缩放 + base64 编码职责。
  - 状态机：`IDLE → SELECTING → RECOGNIZING → OVERLAYING → IDLE`（原 OCRING/TRANSLATING 合并为 RECOGNIZING）。
  - 依赖缩减为：PyQt5 / keyboard / requests（无 Pillow、无 numpy）。
  - 默认模型 `qwen-vl-plus`，设置中可编辑下拉（qwen-vl-max / qwen2.5-vl / qwen3-vl 系列）。
  - 里程碑改为 M0-M5（M2 = 识别翻译一条龙）。
- **已同步更新**：`00-开发规划书.md` / `01-开发计划.md` / `CLAUDE.md` 全部升级至 v1.1。

---

## [批次 A] 项目骨架 + 托盘 + 全局热键 + 选区截图 —— 2026-08-04

- **执行者**：Claude Code
- **涉及文件**：`main.py`、`app/__init__.py`、`app/config.py`、`app/hotkey.py`、`app/screen.py`、`app/selection_overlay.py`、`app/pipeline.py`、`requirements.txt`、`.gitignore`、`tests/test_config.py`、`tests/test_screen.py`
- **完成项**：
  - [x] A1 创建 venv（Python 3.8.9，因系统无 3.11）、安装 PyQt5/keyboard/requests/pytest；git init；目录结构；requirements.txt；.gitignore
  - [x] A2 `main.py`：HighDPI 属性、QSystemTrayIcon（菜单：截图翻译/设置/退出；双击托盘触发；icon.ico 加载兜底程序绘制）、退出时 unregister 钩子
  - [x] A3 `app/hotkey.py`：keyboard 全局热键 + ESC 监听；QTimer.singleShot 切主线程；注册前先 unregister；冲突返回可读错误
  - [x] A4 `app/screen.py`：grab_fullscreen()、ScreenMapper（逻辑↔物理换算）、crop_qimage 返回 QImage、downscale_image（最长边缩放+返回缩放比）、qimage_to_base64_png（QBuffer 编码 PNG→base64 data URI）
  - [x] A5 `app/selection_overlay.py`：全屏半透明遮罩（底图垫底）、拖拽选区 normalized、白色边框+实时尺寸、ESC/右键取消、单击（<3px）无效选区保持遮罩
  - [x] A6 联调代码就绪：热键→遮罩→选区→选区 QImage 存临时 PNG（%TEMP%/ScreenCaptureOCR/）
- **验证结果**：
  - 全部 12 个单元测试通过（6 个 config + 6 个 screen）
  - 所有模块导入无错误
  - 配置读写正常（%APPDATA%/ScreenCaptureOCR/config.json）
  - 需用户在桌面环境实测：托盘图标、热键 Ctrl+Alt+T、选区拖拽、ESC 取消、选区 PNG 输出
- **问题与决策**：
  - 系统仅有 Python 3.8.9，非规格要求的 3.11。经评估，所需库（PyQt5/keyboard/requests）均支持 3.8，`from __future__ import annotations` 兼容类型标注，决定先用 3.8 开发，后续如有兼容性问题再升级
  - venv 内 pip 升级被权限拒绝，不影响正常使用
  - pip install 命令行产生 3 个垃圾文件（`=0.13.5`/`=2.31`/`=5.15.10`），已清理
- **遗留事项**：
  - 用户需在有桌面环境的 Windows 上运行 `python main.py` 实测 GUI 全流程
  - `assets/icon.ico` 尚未提供，程序绘制兜底图标；用户可后续放入自定义图标
- **验收结论**：✅ 通过（代码+测试验证；GUI 需用户桌面实测）

---

## [批次 B+C+D+E+F] 千问视觉 + 设置 + 覆盖层 + 打磨 + 打包 —— 2026-08-04

- **执行者**：Claude Code（一口气连续开发）
- **涉及文件**：`app/qwen_client.py`、`app/settings_dialog.py`、`app/result_overlay.py`、`app/pipeline.py`（重写）、`main.py`（重写）、`ocr_translator.spec`、`build.bat`、`README.md`、`tests/test_qwen_client.py`、`tests/test_result_overlay.py`
- **完成项**：
  - [x] B1-B7 `app/qwen_client.py`：VisionLine/QwenError 数据模型、图片预处理（缩放→base64编码）、结构化 prompt（含图片尺寸+json格式要求）、`response_format: json_object`、鲁棒 JSON 解析（markdown剥离/字段校验/translation 兜底）、坐标换算链（模型 bbox × scale + offset → 屏幕绝对物理坐标）、错误分类（6 种 code）+ 重试策略、`requests.Session` 复用
  - [x] C1-C4 设置对话框：API Key 密码框+显示切换、模型可编辑下拉（5 种已知模型+自定义）、快捷键捕获（`keyboard.hook` 捕获组合键）、颜色选择器、padding/字号/边长微调、保存后自动重注热键
  - [x] D1-D5 译文覆盖层：全屏透明窗口（`WA_TranslucentBackground`+`WindowTransparentForInput`+置顶+不抢焦点）、每行独立背景块+自适应字号中文译文（16pt 递减至 9pt）、中文按字符折行、`QFontMetrics.boundingRect` 测量、一次性 show 防闪烁、ESC 退出
  - [x] E1-E5 打磨：QThread worker（`RecognizeWorker`）完整实现、状态机完整（IDLE→SELECTING→RECOGNIZING→OVERLAYING→IDLE，失败回退+托盘气泡）、防重入、`requests.Session` 连接复用、`shutdown()` 安全退出（`requestInterruption`+`wait(3000)`）、30 个单元测试全部通过
  - [x] F1-F6 PyInstaller 打包：`ocr_translator.spec`（onedir+windowed）、`build.bat`（全自动：打包+精简+体积检查）、后处理精简移除不需要的 Qt 组件（opengl32sw 20MB / Qt5Quick+Qml 7.5MB / d3dcompiler 4MB 等）、`README.md`（安装/配置/API Key 获取/模型说明/计费/FAQ）
- **验证结果**：
  - 全部 **30/30** 单元测试通过（config 6 + screen 6 + qwen_client 14 + result_overlay 4）
  - 所有模块导入无错误
  - PyInstaller 打包成功：**46MB**（远低于 60MB 目标）
  - dist 结构正常：`ScreenCaptureTranslator.exe` + `_internal/`（仅保留 qwindows/qjpeg/qgif/qico 必需插件）
  - 需用户在桌面环境实测：启动 exe、配置 API Key、完整流程验证
- **问题与决策**：
  - PyInstaller 首次打包 92MB，通过后处理脚本移除不需要的 Qt 组件压缩至 46MB
  - 移除的组件：opengl32sw.dll(20MB)、Qt5Quick/Qml(7.5MB)、d3dcompiler_47(4MB)、libGLESv2(3.3MB)、Qt5Network(1.3MB)、Qt5Svg、Qt5DBus、Qt5WebSockets、translations、iconengines、platformthemes 等
  - 保守保留核心组件：Qt5Core/Gui/Widgets、qwindows 平台插件、qjpeg/qgif/qico 图片格式（PNG 为 Qt 内置无需插件）
  - Python 3.8 兼容性良好，所有特性均正常工作
- **遗留事项**：
  - 用户需提供千问 API Key 进行真实 API 联调（设置 → 填入 Key → 截图翻译）
  - `assets/icon.ico` 尚未提供，exe 用程序绘制兜底图标
  - 干净 Windows 机器（无 Python）验证尚未执行
  - 多屏/150% DPI 缩放下需用户实测
- **验收结论**：✅ 代码+测试+打包全通过；API 联调和 GUI 实测需用户在桌面环境完成

---

## [迭代修复] 主窗口 GUI + 热键可靠性 + 模型兼容性 —— 2026-08-04

- **执行者**：Claude Code（根据用户实测反馈逐轮修复）
- **涉及文件**：`app/hotkey.py`、`app/main_window.py`、`app/qwen_client.py`、`main.py`、`tests/`

### 修复 1：主控制面板 GUI（替代纯托盘模式）
- **问题**：用户需要可视化设置界面和翻译日志，而不是只有托盘图标
- **方案**：新建 `app/main_window.py`——完整的控制面板窗口
  - 翻译开关：ON/OFF 按钮，关闭时热键无效
  - 设置区：API Key 输入（密码+显示切换）、模型下拉、快捷键捕获（点击按钮后按组合键）
  - 样式区：背景色/文字色选择器、边距/字号微调
  - 翻译日志：QTableWidget 表格（时间/原文/译文），最多 200 条，自动滚动
  - 关闭窗口 = 最小化到托盘（双击托盘恢复）
  - 首次启动无 API Key 时托盘气泡提醒
- **main.py 适配**：主窗口 + 托盘共存；翻译完成自动记录日志

### 修复 2：热键改用 Win32 RegisterHotKey
- **问题**：keyboard 库在 Win10/11 上有时不响应，用户按 Ctrl+Alt+T 无反应
- **根因**：keyboard 库使用 SetWindowsHookEx 低层钩子，部分 Windows 环境需管理员权限或被安全软件拦截
- **方案**：创建隐藏 1×1 QWidget，通过 `RegisterHotKey(hwnd, ...)` 注册系统级热键，在 `nativeEvent()` 中接收 `WM_HOTKEY`
- **效果**：无需管理员权限，不与 keyboard 钩子冲突，可靠性大幅提升
- **ESC 保留 keyboard 库**：ESC 监听仍用 keyboard（仅覆盖层显示期间有效，冲突风险低）

### 修复 3：翻译 prompt 优化
- **问题 1**：模型只翻译部分单词，专有名词保留英文（如 "ScreenCaptureOCR翻译器"）
- **修复**：prompt 明确要求「把每一个英文单词都翻译成中文，包括专有名词、产品名、技术术语」
- **问题 2**：`response_format: json_object` 要求 `{...}` 对象，prompt 却要 `[...]` 数组——格式冲突
- **修复**：去掉 `response_format`，prompt 改为要求 `{"lines": [...]}` 对象格式；解析优先匹配 `lines` 键
- **问题 3**：选区含中文时模型拒绝返回
- **修复**：prompt 改为「中英文都识别，英文翻译、中文原文保留」

### 修复 4：bbox 格式兼容
- **问题**：模型返回 `[x1,y1,x2,y2]` 矩形两角坐标，代码只认 `[[x1,y1],[x2,y2],[x3,y3],[x4,y4]]` 四角——全部被拒
- **根因**（从日志发现）：`'bbox': [148, 30, 752, 86]` / `'bbox': [82, 10, 174, 25]`
- **方案**：新增 `_normalize_bbox()` 纯函数，自动识别并转换三种格式：
  1. `[[x1,y1],[x2,y2],[x3,y3],[x4,y4]]` 四角 → 原样
  2. `[[x1,y1],[x2,y2]]` 两角 → 展开为四角
  3. `[x1,y1,x2,y2]` 扁平四值 → 展开为四角
  - 零宽度/零高度 bbox 自动丢弃

### 修复 5：响应解析鲁棒性增强
- 优先 `json.loads` 整个内容 → 提取 `lines` 键
- 失败回退到 `[...]` 截取
- 错误信息包含原始响应前 300 字符，方便排查

- **验证结果**：全部 **39/39** 单元测试通过（新增 9 个热键解析测试）
- **打包体积**：46MB（≤60MB ✅）

---

## [首次发布] GitHub 第一次提交准备 —— 2026-08-04

- **事件**：项目本地开发完成，准备首次推送到 GitHub。
- **当前 git 状态快照**：
  - 分支：`master`；本地提交 **7 个**（批次 A → 批次 B-F → 主窗口 GUI → 迭代修复），最近一次 `8856991 docs: 更新开发记录（迭代修复）+ README`
  - 提交者身份：`fzzzz <2677215945@qq.com>`（已配置）
  - **远程仓库：未配置**（`.git/config` 无 `[remote "origin"]`）——首次推送需先创建 GitHub 仓库并添加 remote
- **当前项目完成度**：
  - 全部批次 A-F ✅；迭代修复 ✅；单元测试 **39/39** 通过；PyInstaller 打包 **46MB**（≤60MB 目标 ✅）
  - 遗留实测项（不影响首次提交，但影响发布质量）：真实千问 API 联调、干净机器（无 Python）验证、多屏/150% DPI 实测、`assets/icon.ico` 缺失（程序兜底图标）
- **提交前检查**：
  - [x] `venv/`、`dist/`、`build/`、`config.json`、`__pycache__/` 已在 .gitignore ✅（API Key 不会入库）
  - [x] 本地无敏感信息（config.json 在 %APPDATA%，项目内不存在）
  - [ ] **清理打包残留 spec 文件**：`ScreenCaptureTranslator.spec`、`ScreenCaptureTranslator2.spec`、`ST3.spec`~`ST7.spec`（共 7 个，仅保留 `ocr_translator.spec`）
  - [ ] 确认是否提交 `README.md` 引用的 `ScreenCaptureTranslator.zip`（dist 产物不入库，README 已说明从 release 下载）
- **建议推送流程**：创建 GitHub 空仓库（**不要**勾选自动生成 README/.gitignore）→ `git remote add origin <url>` → `git branch -M main` → `git push -u origin main`
- **验收结论**：⏳ 待用户执行推送后确认

---

## 待填批次（Claude Code 按顺序追加）

- [x] 批次 A：项目骨架 + 托盘 + 全局热键 + 选区截图
- [x] 批次 B：千问视觉一条龙（缩放/编码/请求/JSON 解析/坐标换算）
- [x] 批次 C：配置管理 + 设置界面
- [x] 批次 D：译文覆盖层 + ESC 退出 + 全流程
- [x] 批次 E：打磨（异常/状态提示/DPI/单元测试）
- [x] 批次 F：PyInstaller 打包 + 发布（体积 ≤60MB）

---

## 验收记录（最终发布前签署）

- [ ] 双击 exe 启动，托盘图标可见，无控制台
- [ ] 默认快捷键全屏遮罩、拖拽选区、选区外变暗
- [ ] 松开后 3-10 秒译文覆盖在原文位置，底色遮盖原文
- [ ] 覆盖层鼠标穿透；ESC 立即退出；连续 10 次无崩溃
- [ ] 设置修改后重启生效
- [ ] 断网 / 坏 Key / 模型返回异常 → 友好提示不崩溃
- [ ] 125%/150% 缩放下选区与译文不错位
- [ ] 打包体积 ≤ 60MB（实际 **46MB**）；托盘退出后进程完全结束

*签署人：用户  |  日期：____-__-__*
